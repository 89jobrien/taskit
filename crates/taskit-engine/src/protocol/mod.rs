/// Contract-source normalization and hashing utilities.
pub mod contract_hash;
/// Protocol drift checking, reporting, and lockfile maintenance.
pub mod drift;
/// Construction-site counting checks for protocol invariants.
pub mod sites;
